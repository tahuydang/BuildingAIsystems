def main():
    prompt = "Explain Input -> Processing -> Model -> Output -> Feedback in the context of AI systems.  "

    response = (
        "An AI system takes input, processes it into a usable form, "
        "uses a model to produce an output, and uses feedback to improve future results."
    )

    print("Prompt:", prompt)
    print("Response:", response)

if __name__ == "__main__":
    main()